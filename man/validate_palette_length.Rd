% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{validate_palette_length}
\alias{validate_palette_length}
\title{Internal: Validate palette length filters}
\usage{
validate_palette_length(
  palettes,
  value,
  comparator = c("==", ">=", "<="),
  var_name = "length"
)
}
\arguments{
\item{palettes}{A tibble of palettes (typically \code{aop_palettes}) containing a \code{length} column.}

\item{value}{A single numeric value to validate.}

\item{comparator}{One of \code{"=="}, \code{">="}, or \code{"<="}. Determines how to match \code{value} to palette lengths.}

\item{var_name}{Name of the parameter being validated (e.g., \code{"length"}, \code{"min_length"}), used in error messages.}
}
\value{
Invisibly returns \code{TRUE} if the input passes all checks. Otherwise throws an error.
}
\description{
Checks whether the supplied palette length constraint (\code{length}, \code{min_length},
or \code{max_length}) is valid: must be a single numeric, non-negative value, and
must match at least one palette in \code{aop_palettes} based on the given comparator.
Issues informative errors for invalid inputs, and a warning for unusually large values.
}
\keyword{internal}
