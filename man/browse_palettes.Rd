% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/browse_palettes.R
\name{browse_palettes}
\alias{browse_palettes}
\title{Browse and filter Anatomy of Plots colour palettes}
\usage{
browse_palettes(
  name = NULL,
  mode = NULL,
  mood = NULL,
  tags = NULL,
  length = NULL,
  min_length = NULL,
  sort_by = c("name", "length", "mood"),
  descending = FALSE,
  ncol = NULL,
  nrow = NULL,
  dark_mode = FALSE,
  return = c("plot", "tibble", "both"),
  random = FALSE
)
}
\arguments{
\item{name}{Optional character string to partially match palette names.}

\item{mode}{Optional character vector restricting palettes to the given
background mode(s): "light", "dark", or "both".}

\item{mood}{Optional character vector of moods to include.}

\item{tags}{Optional character vector of tags. Only palettes containing
all supplied tags are returned.}

\item{length}{Optional integer. If supplied, only palettes with exactly
this number of colours are returned.}

\item{min_length}{Optional integer giving the minimum number of colours
a palette must contain to be returned.}

\item{sort_by}{Variable to sort by. One of "name", "length", or "mood".}

\item{descending}{Logical; if \code{TRUE} sort in descending order.}

\item{ncol, nrow}{Optional layout settings passed to \code{ggplot2::facet_wrap}
when a plot is produced.}

\item{dark_mode}{Logical. If \code{TRUE}, uses a dark background for the plot.}

\item{return}{One of "plot", "tibble", or "both" indicating the desired
output type.}

\item{random}{Logical; if \code{TRUE}, randomises the order of palettes after
filtering.}
}
\value{
A ggplot object, a tibble, or a list containing both depending on
the value of \code{return}.
}
\description{
Provides a visual overview of palettes stored in \code{aop_palettes}.
Users can filter palettes by name, mode, mood, tags or length and
optionally sort the results. The output can be returned as a plot,
a tibble, or both.
}
\examples{
browse_palettes(mode = "dark", tags = c("storm"))
}
